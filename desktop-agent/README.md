# Sobhan Notification Hub Desktop Agent

این برنامه به زیرساخت `SobhanNotificationHub` متصل می‌شود. Agent با device token به `/api/notify/pending.php` وصل می‌شود و deep link هر اعلان را از `action_url` باز می‌کند.

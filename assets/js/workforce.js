document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('[data-doc-editor]').forEach(editor=>{const surface=editor.querySelector('[data-editor-surface]'),input=editor.querySelector('[data-editor-input]');editor.querySelectorAll('[data-command]').forEach(button=>button.addEventListener('click',()=>{surface.focus();document.execCommand(button.dataset.command,false,button.dataset.value||null)}));editor.closest('form')?.addEventListener('submit',()=>{input.value=surface.innerHTML})})});

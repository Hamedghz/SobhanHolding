<?php
require_once __DIR__.'/../core/Response.php';require_once __DIR__.'/../services/SalesOfferBudgetService.php';SalesOfferBudgetService::repairSchema();Auth::requireLogin();if(!SalesOfferBudgetService::isAdmin()){http_response_code(403);die('دسترسی غیرمجاز');}$error='';
if ($_SERVER['REQUEST_METHOD'] === 'GET') redirect('/admin/formula-builder.php?category=offer_budget');
if ($_SERVER['REQUEST_METHOD'] === 'POST') { http_response_code(403); exit('ویرایش مستقیم فرمول قدیمی غیرفعال است؛ از فرمول‌ساز تصویری استفاده کنید.'); }
if($_SERVER['REQUEST_METHOD']==='POST')try{if(!Auth::verifyCsrf($_POST['csrf_token']??null))throw new InvalidArgumentException('درخواست معتبر نیست.');$rate=$_POST['default_offer_rate']??'';if(!is_numeric($rate)||(float)$rate<0)throw new InvalidArgumentException('نرخ پیش‌فرض معتبر نیست.');Database::execute('UPDATE sales_offer_formula_settings SET settings_json=?,updated_at=NOW() WHERE formula_key="offer_budget_provisional"',[json_encode(['default_offer_rate'=>(float)$rate],JSON_UNESCAPED_UNICODE|JSON_PRESERVE_ZERO_FRACTION)]);flash('تنظیمات فرمول ذخیره شد.');redirect('/admin/sales-offer-formula-settings.php');}catch(Throwable $e){$error=SalesOfferBudgetService::uiError($e,'ذخیره تنظیمات انجام نشد.');}
$setting=SalesOfferBudgetService::getAvailableFormulaSettings()[0]??[];$json=json_decode((string)($setting['settings_json']??''),true)?:[];$pageTitle='تنظیمات فرمول بودجه آفر';require __DIR__.'/../views/partials/admin-header.php';?>
<h1>تنظیمات فرمول بودجه آفر</h1><?php if($error):?><div class="alert alert-danger"><?=e($error)?></div><?php endif;?><form class="card admin-form" method="post"><input type="hidden" name="csrf_token" value="<?=e(Auth::csrfToken())?>"><h2>provisional_v1</h2><p class="muted">بودجه مقدماتی = قیمت خرید × تعداد آفر × نرخ آفر</p><label class="form-field"><span>نرخ پیش‌فرض موقت</span><input type="number" min="0" step="0.0001" name="default_offer_rate" value="<?=e((string)($json['default_offer_rate']??0))?>"></label><button class="btn btn-primary">ذخیره</button></form><section class="card"><h2>فرمول نهایی</h2><p>فرمول نهایی پس از دریافت فایل Excel تکمیل می‌شود.</p></section><?php require __DIR__.'/../views/partials/admin-footer.php';?>

<?php
require_once __DIR__.'/../core/Auth.php';require_once __DIR__.'/../core/Response.php';require_once __DIR__.'/../core/SalesOfferBudgetModule.php';
if(PHP_SAPI!=='cli'){Auth::requireLogin();if(!Auth::canManageSystemTools()){http_response_code(403);die('مجوز مدیریت سیستم لازم است.');}if($_SERVER['REQUEST_METHOD']!=='POST'){$pageTitle='تعمیر استعلام بودجه آفر';require __DIR__.'/../views/partials/admin-header.php';?><section class="card admin-form"><h1>تعمیر استعلام بودجه آفر</h1><p class="muted">جداول و تنظیمات به‌صورت افزایشی و بدون حذف اطلاعات بررسی می‌شوند.</p><form method="post"><input type="hidden" name="csrf_token" value="<?=e(Auth::csrfToken())?>"><button class="btn btn-primary">اجرای تعمیر</button></form></section><?php require __DIR__.'/../views/partials/admin-footer.php';exit;}if(!Auth::verifyCsrf($_POST['csrf_token']??null)){http_response_code(419);die('درخواست معتبر نیست.');}}
try{SalesOfferBudgetModule::repair(Database::connection());$message='ساختار استعلام بودجه آفر با موفقیت بررسی شد.';if(PHP_SAPI==='cli')echo $message.PHP_EOL;else{flash($message);redirect('/admin/install-tools.php');}}catch(Throwable $e){error_log('Sales offer budget repair: '.$e->getMessage());$message='بررسی ساختار استعلام بودجه آفر انجام نشد.';if(PHP_SAPI==='cli'){fwrite(STDERR,$message.PHP_EOL);exit(1);}flash($message,'danger');redirect('/admin/install-tools.php');}

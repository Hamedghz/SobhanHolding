<?php
require_once __DIR__.'/../core/Auth.php';require_once __DIR__.'/../core/Response.php';require_once __DIR__.'/../core/HrAttendanceModule.php';
if(PHP_SAPI!=='cli'){Auth::requireLogin();if(!Auth::canManageSystemTools()){http_response_code(403);exit('دسترسی غیرمجاز');}if($_SERVER['REQUEST_METHOD']!=='POST'){$pageTitle='راه‌اندازی حضور و کارکرد';require __DIR__.'/../views/partials/admin-header.php';?><section class="card"><h1>راه‌اندازی حضور و کارکرد پرسنل</h1><p>جداول، گروه‌های کاری و view خلاصه KPI فقط به‌صورت افزایشی و قابل اجرای مجدد ایجاد می‌شوند.</p><form method="post"><input type="hidden" name="csrf_token" value="<?=e(Auth::csrfToken())?>"><button class="btn btn-primary">اجرای نصب امن</button></form></section><?php require __DIR__.'/../views/partials/admin-footer.php';exit;}if(!Auth::verifyCsrf($_POST['csrf_token']??null)){http_response_code(419);exit('درخواست معتبر نیست.');}}
try{HrAttendanceModule::repair(Database::connection());if(PHP_SAPI==='cli')echo "ساختار حضور و کارکرد تکمیل شد.\n";else{flash('ساختار حضور و کارکرد با موفقیت تکمیل شد.');redirect('/admin/hr-attendance-settings.php');}}catch(Throwable $e){error_log('HR attendance install: '.$e->getMessage());if(PHP_SAPI==='cli'){fwrite(STDERR,"نصب ناموفق بود.\n");exit(1);}flash('راه‌اندازی ماژول انجام نشد.','danger');redirect('/admin/install-tools.php');}

<?php
require_once __DIR__.'/../core/Auth.php';require_once __DIR__.'/../core/Response.php';require_once __DIR__.'/../core/ManagementMeetingsModule.php';if(PHP_SAPI!=='cli'){Auth::requireLogin();if(!Auth::canManageSystemTools()){http_response_code(403);exit('دسترسی غیرمجاز');}if($_SERVER['REQUEST_METHOD']!=='POST'){$pageTitle='راه‌اندازی قوانین و صورتجلسات';require __DIR__.'/../views/partials/admin-header.php';?><section class="card"><h1>راه‌اندازی قوانین مصوب و صورتجلسات</h1><p>ساختار جداول فقط به‌صورت افزایشی بررسی می‌شود.</p><form method="post"><input type="hidden" name="_csrf" value="<?=e(Auth::csrfToken())?>"><button class="btn btn-primary">اجرای نصب امن</button></form></section><?php require __DIR__.'/../views/partials/admin-footer.php';exit;}if(!Auth::verifyCsrf($_POST['_csrf']??null)){http_response_code(419);exit('درخواست معتبر نیست.');}}try{ManagementMeetingsModule::repair(Database::connection());if(PHP_SAPI==='cli')echo "ساختار ماژول تکمیل شد.\n";else{flash('ساختار ماژول با موفقیت تکمیل شد.');redirect('/admin/management-meetings.php');}}catch(Throwable $e){error_log('Management meetings install: '.$e->getMessage());if(PHP_SAPI==='cli'){fwrite(STDERR,"نصب ناموفق بود.\n");exit(1);}flash('راه‌اندازی ماژول انجام نشد.','danger');redirect('/admin/install-tools.php');}
